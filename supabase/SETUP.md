# Supabase

La guía vigente de cuentas, permisos, despliegue y comprobaciones está en [docs/operacion.md](../docs/operacion.md). El mapa de tablas y las reglas de privacidad están en [docs/arquitectura.md](../docs/arquitectura.md).

`schema.sql` es el esquema inicial y no debe ejecutarse de nuevo sobre un proyecto ya configurado. Las migraciones posteriores son archivos separados de esta carpeta.
